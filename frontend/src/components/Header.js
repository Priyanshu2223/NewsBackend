import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div className="top-section">
      {/* Removed the "date" and "login-subscribe" sections */}
    </div>
  );
};

export default Header;
